{
  "name": "birc-utils",
  "version": "0.1.0",
  "private": true,
  "description": "A collection of utility scripts for bIRC.",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
